# Storybook Testing Package Scope

`@storybook/testing-library` and `@storybook/jest` are deprecated, but their replacement `@storybook/test` has a Storybook 8 peer. This repo is on Storybook 6.5, so replacing those packages belongs in a Storybook 8+ migration plan.
